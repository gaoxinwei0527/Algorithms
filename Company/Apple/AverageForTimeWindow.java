package Company.Apple;

public class AverageForTimeWindow {
    /*
    design some functions:
    void init(int window_size()    //set a latest window size, e.g. 5 minutes
    void put(int long, int long)     //put a key-value pair
    long get(int long)                  //give a key, return a value
    double getAvg()                    //return the average of all values in the latest window
    all operations must be in O(1) runtime, design on a single machine first (a lot of implementation details), then how to scale it to clusters.
    */
}
